# Main domain MongoDB
MONGODB_URI_MAIN=mongodb+srv://user:password@cluster.mongodb.net/agent-ai-main

# Insure24 domain MongoDB
MONGODB_URI_INSURE24=mongodb+srv://user:password@cluster.mongodb.net/agent-ai-insure24

# Shared Gemini API Key
GEMINI_API_KEY=your-gemini-api-key

# JWT Secret (can be shared or separate per domain)
JWT_SECRET=your-jwt-secret

# Add any other environment variables your app needs
# NEXT_PUBLIC_FIREBASE_*=...
